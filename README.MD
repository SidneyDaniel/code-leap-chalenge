# Como executar o programa


Execute o seguinte comando na raiz do seu projeto (onde está a pasta backend):


docker run --rm -v "$PWD/backend":/usr/src/app -w /usr/src/app django django-admin startproject codeleap_backend .




    